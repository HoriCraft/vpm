# 既定はテキストとして扱い、リポジトリには LF で保存する
* text=auto eol=lf

# ワークフローの run: ブロックは Linux の bash が実行する。
# CRLF が混じると $'\r': command not found で落ちるため、必ず LF に固定する。
*.yml  text eol=lf
*.yaml text eol=lf
*.sh   text eol=lf
